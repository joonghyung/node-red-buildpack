#!/usr/bin/env bash
# bin/detect <build-dir>

BP=$(dirname "$(dirname $0)")

ls $1/flow_*.json >/dev/null 2>&1
if [ $? -eq 0 ]; then
  mv $1/flow_*.json $1/flow.json
fi

if [ -f "$1/flow.json" ]; then
  echo "node.js "$(cat "$BP/VERSION")""
  exit 0
fi

exit 1
